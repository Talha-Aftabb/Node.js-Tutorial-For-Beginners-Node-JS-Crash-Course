// __filename (We get the root of the filename through this function)
// __dirname (We get the root directory through this function)

const GetUserInfo = require("./sayName");

GetUserInfo.SayAddress();
GetUserInfo.SayName();
